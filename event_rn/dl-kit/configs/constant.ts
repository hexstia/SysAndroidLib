

/**
*  常量
*/

const constact = {
    locationSaveKey: {
        userInfo: 'userInfo',
        refreshToken: 'refreshToken',
        token: 'token',
        showAgreementModal: 'showAgreementModal'
    }
};

export default constact